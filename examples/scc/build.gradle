/*
 * This build file was auto generated by running the Gradle 'init' task
 * by 'mp' at '28/10/15 08:41' with Gradle 2.8
 *
 * This generated file contains a sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at https://docs.gradle.org/2.8/userguide/tutorial_java_projects.html
 */

// Apply the java plugin to add support for Java
apply plugin: 'java'
apply plugin: 'maven'
apply plugin: 'eclipse'

version = '0.1'
sourceCompatibility = 1.7
targetCompatibility = 1.7
group = 'org.mp'
jar.duplicatesStrategy = DuplicatesStrategy.EXCLUDE
 

// In this section you declare where to find the dependencies of your project
repositories {
    // Use 'jcenter' for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
    mavenLocal()
}

// In this section you declare the dependencies for your production and test code
dependencies {
     //em4so
    compile 'org.mp:em4so:0.1'
    compile 'ch.qos.logback:logback-classic:1.0.13'
    testCompile 'junit:junit:4.12'
}

task fatJar(type: Jar) {
	duplicatesStrategy = 'exclude'
  manifest {
    attributes 'Implementation-Title': 'Smart Cleaning Cupboard', 'Implementation-Version': version

  }
    baseName = project.name + '-all'
    from {
    // Include all dependencies in the jar
    configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
  	}
    with jar
}
install {
    repositories.mavenInstaller {
        pom.artifactId = project.name 
        pom.packaging = 'jar'
        pom.version = version
    }
}
